use "std-stdio"

extern fn printf(msg: c_string)

fn main() =>
    const y = (2 + 3) * 5

    match y
        0, 1 => printf("Zero or One")
        2 => printf("2")
    else
        printf("Other")
    end