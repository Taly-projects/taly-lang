pub space Number

    pub class I32
        var inner: c_int

        pub new create(inner: c_int) =>
            self.inner = inner
            self.add_one()

        pub fn add_one() =>
            self.inner = self.inner + 1

pub space Test

    pub fn zero(): I32 => Number.I32.create(0)

pub intf ToCString

    pub fn to_c_string(): c_string

fn main() =>
    const x = Number.I32.create(0)
    x.add_one() 